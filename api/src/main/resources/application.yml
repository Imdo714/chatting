server:
  port: 8080

spring:
  application:
    name: application

  datasource:
    url: jdbc:mysql://localhost:3306/modular
    username: root
    password: MYSQL1w!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true